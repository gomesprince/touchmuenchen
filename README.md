# Touch Rugby Demo Landing Page

A responsive static landing page demo for a touch rugby team hub.

## Files

```text
index.html
assets/css/styles.css
assets/js/main.js
assets/img/hero-rugby.svg
assets/img/training.svg
.nojekyll
```

## Run locally

Open `index.html` directly in a browser, or run a simple local server:

```bash
python -m http.server 8000
```

Then open:

```text
http://localhost:8000
```

## Host on GitHub Pages

1. Create a GitHub repository.
2. Upload all files from this folder to the repository root.
3. Go to **Settings → Pages**.
4. Choose **Deploy from a branch**.
5. Select the `main` branch and `/root` folder.
6. Save and wait for GitHub Pages to publish the site.

## Notes

- This is a static demo page. The contact form does not send messages yet.
- Replace the placeholder text with the real team details later.
- No build step is required.
